# Проверка P0 pipeline

Дата: 2026-09-23. Основа: `origin/main` / `0d29565`. Изолированная ветка: `codex/real-semantic-demo`.

| Команда / проверка | Exit code | Результат |
| --- | --- | --- |
| `node --import tsx backend/run-synthetic.ts` | 0 | 5 units, 10 functions, `isMock=false` |
| `npm test` | 0 | 33 теста, 7 файлов |
| `npm run typecheck` | 0 | Без ошибок |
| `npm run lint` | 0 | Без ошибок и предупреждений |
| `npm run build` | 0 | Production build успешен |
| Node fetch: multipart POST и GET production API, порт 3137 | 0 | POST 202; GET 200; `done`; все 7 stages `done`; `isMock=false` |
| `git diff --check` | 0 | Без ошибок пробелов |
| Ручной UI-прогон браузером | NOT RUN | Проверены production HTTP API и существующие frontend contract tests |
| Основной TXT-корпус редакций 8/9 | NOT RUN | TXT и сложные владельцы не поддержаны текущим минимальным pipeline |

Ни expected-results, ни expected JSON не читались. Тестовые утверждения synthetic зафиксированы после первого runtime-output и сопоставлены с заданием пользователя. [Полный фактический ответ API](synthetic-api-output.json).

| Случай | Вычисленный результат | Источники |
| --- | --- | --- |
| S01 | RENAMED (transformed): внутренний контроль → комплаенс и внутренний контроль | before/after §2; совпадающие функции приложены к evidence |
| S02 | UNCHANGED: мониторинг договорных обязательств | before §2.1, after §2.1 |
| S03 | POSSIBLE_LOSS / LOSS: соблюдение процедур закупок | before §2.2; проверены все 5 AFTER-функций и исходные AFTER-фрагменты |
| S04 | UNCHANGED: мониторинг корректирующих мероприятий | before §2.3, after §2.2 |
| S05 | UNCHANGED: выявление и оценка корпоративных рисков | before/after §3.1 |
| S06 | DUPLICATION: контроль мероприятий по управлению рисками у двух AFTER-владельцев | after §3.2 (риск-менеджмент), §4.1 (корпоративное управление) |

В synthetic нет подтверждённого конфликтного сочетания: CONFLICT=0. Отдельный регрессионный тест проверяет потенциальный конфликт исполнения и контроля одного процесса. Другие тесты проверяют перенос между владельцами, слияние нескольких BEFORE-назначений в одно AFTER без ложной потери, различия объектов/периодичности/отрицания, частичное покрытие и границы разделов.

Блокеров для проверенного synthetic demo нет. Для QA: интегрировать commit и запускать без `NEXT_PUBLIC_USE_MOCK=true`; upload одного DOCX на сторону достаточен. Изменения frontend и контракта не требуются. Основной корпус и универсальное семантическое извлечение остаются ограничениями, описанными в [backend README](../README.md).
